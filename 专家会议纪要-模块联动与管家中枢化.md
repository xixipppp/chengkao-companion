# 专家会议纪要 · 模块联动与「AI 管家中枢化」专项研讨

**会议时间**：2026-09-26（紧急召集）
**召集人**：心动成考·乙女陪练 项目组
**议题（用户原话）**：「从用户角度出发，感觉各个功能模块和 AI 管家之间应该有更强的联动，面向客户应该能更优化提升学习效率的体验——你觉得呢？联合！所有专家开个会议。」

**与会专家（6 人）**
| 代号 | 角色 | 关注点 |
|---|---|---|
| UX | 用户研究员（新增席位，因本次议题以用户视角切入） | 首次决策成本、路径摩擦、认知负荷 |
| LS | 学习科学专家 | 迁移、间隔、认知负荷、动机内化 |
| EX | 成考命题专家 | 分值 ROI、必拿分、易错陷阱 |
| GM | 游戏化与心流设计专家 | 可变奖励疲劳、反模式、正反馈 |
| NS | 乙女向叙事与陪伴设计专家（新增席位） | 角色动机与学习行为的绑定 |
| AI | AI 工程与交互架构专家 | 数据流、状态复杂度、API 成本、降级 |

**研讨素材**：`index.html` 现行代码结构（19 个页面区块、10 个小游戏、2327 题题库、管家 5 个标签页）。此为**基于真实代码事实**的研讨，非泛论。

---

## 一、主持人（我）的开场判断

> 应用现在的问题不是"功能不够"，而是**功能之间没有互相认识**。
> 管家是一个**目的地**（要用户主动想到它、点进去），而不是一个**操作系统**（应当指挥所有模块）。
> 这一层「编排层」缺失，是当前学习效率体验的最大漏损点。**我同意用户的判断。**

---

## 二、现状诊断：五处真实断点（基于代码事实）

### 断点 1｜管家是页面，不是中枢
`s-butler` 有 5 个标签（仪表盘/排课/诊断/冲刺/加冕之路），但**只能被用户在首页/自习室主动点进去**。做题页、小游戏、动画课堂、世界树里，管家**完全不存在**。
→ **用户需要先"想起来"才有服务，这是被动式设计。**

### 断点 2｜数据单向流入，从不流出
`recordAnswer` → `st.records` → `renderDash()`。链路是**单向的**。
管家算出的"薄弱考点"，**无法反向注入**任何模块：
- 小游戏用各自固定池（`MATCH_POOLS` / `ALLQ` 随机），与你错什么**毫无关系**；
- 动画课堂是固定课文；
- 考点速记/世界树是只读静态。

### 断点 3｜完成动作后没有回流
- 小游戏结算（`#gReplay`）后，管家不知道你玩过、玩得怎样，排课不变；
- 动画课堂课后练习错题进了错题池（已落地），但**管家不知道、当日诊断不体现、排课不调整**。
→ 「学了什么」与「接下来学什么」之间断了。

### 断点 4｜6 位角色与学习完全解耦（乙女向最大机会）
`getMate` / `addAff` 只统计互动好感；`MOD2MATE` 明明已有"科目 → 角色"映射（可复用于 AI 讲师人设），但**角色×科目×进度三者互不知情**。
→ 结果是：点角色涨好感，学习涨 XP，**两条独立数值线**。情感动机没有传导到学习行为上。

### 断点 5｜首屏没有"唯一指令"
首页并列「3 天倒计时卡 + 管家卡 + 动画课堂卡 + 游乐场按钮 + 攻略同伴」，自习室一屏 **12 个按钮**。
→ **用户知道该学，但不知道该点哪一个**；选择权过载 → 决策瘫痪 → 关掉 App。

---

## 三、专家发言（含交锋）

**UX（用户研究员）**
用户的真实抱怨不是"功能少"，是"我不知道现在该干嘛"。我做了路径盘点：从首页到达"今天真正该做的那件事"，最长路径是 **首页 → 自习室 → 管家 → 排课 → 自己判断 → 回自习室 → 选模式 → 开始**，共 7 步。而用户是在职成年人、碎片时间打开手机。**必须把路径压到 1 步。**

**AI（工程架构）**
技术上这叫缺少**编排层（orchestrator）**。现在每个模块自己选题、自己判定、自己存数据，互相不认识。要做联动，得先统一一个**任务对象**（Task），让所有模块都认这个 schema——管家生产 Task，模块消费 Task，消费完回写结果。这不是小重构，但可以用"旁挂接口"的方式渐进做，不必推翻现有代码。

**EX（命题专家）**
我支持，但提醒一点：**联动的方向必须服从分值 ROI**。不能因为"联动看起来酷"就把时间导到低产出的模块上。管家的唯一指令必须自带分值权重——政治选择题优先，英语阅读次之，高数选择填空再次，作文只保底。

**LS（学习科学专家）**
我关心两个机制。第一，**错题注入小游戏**在学理上是好的——这叫**变式练习（varied practice）**，同一考点换情境，比原地重考迁移效果强得多。但有红线：**必须换情境，不能原题照搬**，否则玩家会发现"这不我刚做错那道吗"，游戏体验和学习效果双输。第二，**角色绑定科目**在动机上要小心「过度理由效应」——如果"陪某角色"只涨好感不涨能力，用户会为了好感偏科。建议：陪学任意科都涨好感，但**该角色擅长科有专属增益叠加**，避免诱导偏科。

**GM（游戏化专家）**
我举双手同意联动，但**反对把游戏变成刷题器**。错题注入比例要设上限，我建议 **≤ 40%**，其余保持游戏原有的随机新鲜感——否则 10 个小游戏就退化成"换了皮的错题本"，可变奖励疲劳会提前到来（上一次会议我已经警告过）。另外我要一个东西：**通关战报**。现在通关出导图口诀，回首页却什么都没留下。玩家需要即时回执。

**NS（乙女叙事设计）**
这是本产品**唯一别人抄不走的护城河**。市面上所有成考 App 都能做出题库和错题本，但**"和谁一起熬过这一晚"是只有我们能做的**。我强烈建议：好感度的增长来源，从"点一点角色"改成"**一起完成一次任务**"。让好感变成"共同经历"的凭证，而不是"戳宠物的积分"。这一点能让留存和情感黏性拉开代差。

**UX ← 反诘 NS**：但角色绑定科目会不会让用户在"最爱角色"和"最弱科目"之间纠结？
**NS 回应**：不冲突。角色提供的是**陪伴与专属内容**（专属口诀、专属出题口吻），不是"只教你那一科"。做题时 AI 讲师沿用 `MOD2MATE` 的人设即可，任何科目都由"当前陪你的角色"来授课。

**AI ← 反对 LS 的闭环 3 全自动重排**：课堂暴露薄弱点后自动推翻当日排课，会引入状态复杂度，用户回来发现计划全变了，会有失控感。
**UX 附议 AI**：对，用户讨厌计划被悄悄改掉。
**LS 让步**：折中——**只在"当前时段之后"插入一个 10 分钟巩固块，不推翻已过的块，并明确告知"我帮你加了 10 分钟"并允许一键撤销。**

---

## 四、共识结论：三条设计原则

> ### 原则 1｜管家从「目的地」变「操作系统」
> 任何模块都必须能被管家**唤起**（注入它的任务），也必须能向管家**回写**（上报它的结果）。管家不再是你要走去的地方，而是**贯穿所有页面的那一层**。

> ### 原则 2｜每个模块必须开「双向接口」
> 入口能接收编排指令（学什么、学几道、什么难度），出口能上报结构化结果（对了什么、错了什么、用了多久）。**只接不回的模块是本轮要修的对象。**

> ### 原则 3｜首屏唯一指令优先
> 任何时刻，首页只强调**这一件事**（今日唯一任务），其余全部降级为"可探索"。选择权过载是当前最大的隐性流失点。

---

## 五、落地方案：五个闭环（附代码落点）

### 闭环 ①「今日唯一任务卡」——治首屏决策瘫痪 【P0】
- **机制**：首页置顶一张**唯一任务卡**，由管家按"当权重叠加"生成：`科目 + 考点 + 题量 + 预计分钟 + 一个「开始」按钮`。点开始直接进 `startDrill(mod, 'normal')`，并携带 `S.planTask` 标记本次来自管家排课。
- **完成回执**：通关后卡片变为「✅ 今日已完成 · 明早 09:00 再来」，并附角色一句评价。
- **落点**：`renderHome` 新增 `renderTodayCard()`；复用 `genDailyPlan()` 当前时段块；`homeResume` 让位（降级为次要提示）。
- **收益**：7 步路径 → **1 步**；首屏认知负荷立降。

### 闭环 ②「弱点注入游戏池」——让小游戏真正练到你的错 【P1】
- **机制**：小游戏出题前，先问管家要"该游戏对应的薄弱项"：
  | 游戏 | 注入内容（来自管家诊断） |
  |---|---|
  | `lim` 极限秒答 / `harvest` 核心收割机 | 高数薄弱考点题 |
  | `match` 公式连连看 | 你错过的公式对 |
  | `flip` 词汇翻牌 | 你错过的英语词汇 |
  | `order` 步骤接龙 | 你错的解题步骤链 |
  | `mine` 陷阱扫雷 | 你踩过的陷阱类题 |
  - **注入比例 ≤ 40%**（GM 红线），其余保持原池随机；**换情境不复刻原题**（LS 红线）。
- **回写**：结算时把对错 `recordAnswer` 回写 `st.records`。
- **落点**：新增 `gamePoolOf(gameId)` → 优先取 `st.records` 薄弱项，fallback 原池；各 `gameXxx` 抽题处改调它。
- **收益**：玩游戏 = 刷自己的错题，不再是"练得开心但没用"。

### 闭环 ③「课堂 → 诊断 → 排课」三角回流 【P1】
- **机制**：动画课堂结束 → 自动生成课后 3 题 → 全对则该考点掌握度 +，错则：
  1. 管家**当日诊断报告顶部**加一条「今日课堂暴露：定语从句」；
  2. **在当前时段之后**插入一个 **10 分钟**该考点巩固块（不推翻已过块），并提示「我帮你加了 10 分钟，点此撤销」（LS×UX 折中）。
- **落点**：`openLesson` 收尾调用 `butlerOnLessonResult(mod, wrongIds)` → 更新 `st.records` + 局部重排 `genDailyPlan()` 剩余块。

### 闭环 ④「角色 × 科目 × 进度」三位一体——乙女向护城河 【P2】
- **机制**：复用已有 `MOD2MATE` 映射：
  1. **好感来源升级**：`addAff` 从"纯互动"扩展到"完成任务/通关"——好感 = 共同经历的凭证（NS 主张）；
  2. **专属增益**：与擅长该科的角色一起学，该科获得专属口诀/专属人设口吻；
  3. **防偏科**：陪学**任意科**都涨好感，仅"擅长科"有增益叠加（LS 红线）。
- **落点**：`startDrill` 读取当前 mate → 注入该科专属文案；`drillVerdict` 通关分支补 `addAff`。
- **收益**：把情感动机真正传导到学习行为，形成他人无法复制的黏性。

### 闭环 ⑤「通关战报回流首页」——即时回执 【P0】
- **机制**：通关 → 首页顶部「战报条」保留 24h：`本轮消灭 X 题 · 用时 Y 分 · 掌握度 ↑Z% · 角色评价一句`；并给两个一键动作：「再来一轮同考点」「看思维导图」。
- **落点**：`drillVerdict` 通关分支写 `st.lastReport` → `renderHome` 渲染。
- **收益**：填补当前"做完无痕"的反馈真空（GM 主张），把成就感带回首屏。

---

## 六、优先级与排期建议

| 优先级 | 闭环 | 理由 | 改动量 |
|---|---|---|---|
| **P0** | ① 今日唯一任务卡 + ⑤ 通关战报回流 | 直接解决首屏决策瘫痪与反馈真空，改动小、见效快 | 小 |
| **P1** | ② 弱点注入游戏池 + ③ 课堂→诊断→排课回流 | 让"练"与"诊断"成环，学习效率提升最实质 | 中 |
| **P2** | ④ 角色×科目×进度三位一体 | 内容量大，但为最高护城河，建议单独排期 | 大 |

**建议路径**：先做 P0 验证"唯一指令 + 即时回执"的留存效果，再把 P1 的闭环接上，最后用 P2 筑护城河。**不要四个一起上**——AI 专家明确警告状态复杂度。

---

## 七、度量指标（上线后观察）

| 指标 | 现状（估） | 目标 |
|---|---|---|
| 首屏到"开始学习"步数 | 7 步 | **1 步** |
| 首屏决策时间 | — | 显著下降 |
| 模块间跳转深度 | 浅（互不连通） | 形成 2 个以上闭环 |
| 错题复现率（同考点再错） | — | 下降 |
| 日任务完成率 | — | 提升 |
| 玩小游戏练到的"自己的错题"占比 | **0%** | ≥ 30%（上限 40%） |

---

## 八、风险与边界（工程约束）

1. **状态复杂度**：四个闭环全上会让 `st` 状态机膨胀 → 采纳 AI 专家意见，**分阶段上线**，每次只接 1–2 个闭环。
2. **AI 成本**：闭环③自动出题会增加 API 调用 → 沿用 v2.1.0 已落地的 `st.aiStats` 缓存与 5 道封顶机制，优先本地 `localStepQuiz` 降级。
3. **计划失控感**：重排只动"未到的时段"，且可撤销（UX 强制要求）。
4. **游戏纯度**：注入比例 ≤ 40%，换情境不复刻（GM + LS 双红线）。
5. **偏科诱导**：角色增益只在"擅长科"叠加，陪学任意科均涨好感（LS 红线）。

---

## 九、一句话总结

> **这个应用不缺功能，缺的是"把这些功能串起来的那根线"。那根线就是 AI 管家——它应该从"你要走去的一个页面"，变成"贯穿所有页面的一层"。做完这五个闭环，用户的体验会从「我有十个工具」变成「我有一个教练」。**

---

## 附：会议分歧记录（供后续复核）

| 分歧点 | 主张方 | 反对方 | 最终裁决 |
|---|---|---|---|
| 课堂暴露薄弱点是否自动重排当日计划 | LS | AI / UX | 折中：只插入 10 分钟未到时段块，可撤销 |
| 错题注入小游戏是否损害游戏性 | GM | LS | 折中：注入上限 40%，换情境不复刻 |
| 角色是否绑定单一科目 | NS（强绑定） | LS / UX（防偏科） | 折中：擅长科有增益叠加，陪学任意科涨好感 |

**记录人**：AI 工程与交互架构（AI）
**下次跟进**：P0 两项（闭环①⑤）开发完成后，复会验证首屏决策步数与任务完成率。
